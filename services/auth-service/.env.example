# Configuration du serveur SMTP pour l'envoi d'emails
# Par défaut, les emails sont simulés (affichés dans les logs)
# Pour activer l'envoi réel, configurez ces variables

# Gmail SMTP (exemple)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@edupath.edu

# Pour Gmail : Créez un "App Password" dans votre compte Google
# Sécurité → Validation en 2 étapes → Mots de passe d'application

# Autres fournisseurs SMTP populaires :
# 
# Outlook/Hotmail :
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# 
# Yahoo :
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# 
# SendGrid :
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
